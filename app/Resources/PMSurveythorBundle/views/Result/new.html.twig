{% extends '::base.html.twig' %}

{% block navbar %}{% endblock %}
{% block body %}

    <h4>{{ app.session.get('survey').title }}</h4>

    <div id="result">
        <button
            class="btn btn-default"
            id="survey-next"
            data-next-url="{{ path('result_next', { 'id' : app.session.get('survey').id, 'item' : item.id }) }}"
        >
            weiter
        </button>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="application/javascript" src="{{ asset('build/result.js') }}"></script>
{% endblock %}

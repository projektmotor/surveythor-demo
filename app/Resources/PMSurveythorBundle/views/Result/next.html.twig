<b>{{ form.vars.value.question.text }}</b>
{% if form is defined %}
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form(form) }}
    {{ form_end(form) }}
{% endif %}

{% if text is defined %}
    <p>
        {{ text }}
    </p>
{% endif %}

<button
    class="btn btn-default"
    id="survey-next"
    data-next-url="{{ path('result_next', { 'id' : app.session.get('survey').id, 'item' : item.id }) }}"
>
    weiter
</button>

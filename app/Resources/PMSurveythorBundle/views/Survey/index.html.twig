{% extends '::base.html.twig' %}

{% block body %}
    {% for survey in surveys %}
        <div>
            {{ survey.title }}&nbsp;
            <a href="{{ path('survey_edit', {'id' : survey.id}) }}">edit</a>
        </div>
    {% endfor %}
{% endblock %}
